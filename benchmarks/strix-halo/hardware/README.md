# Hardware capture

`hardware.json` in every timestamped result directory is generated from read-only
OS, package, Vulkan, memory, GTT, power-profile, ACPI, z13ctl, runtime, and model
identity probes. This directory intentionally contains no tuning scripts.
